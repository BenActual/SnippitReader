<Window x:Class="SnippitReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Snippit Reader – Stage 1" Height="520" Width="820">
    <Grid Margin="12" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="SnipBtn" Click="SnipBtn_Click" Content="Start Snip (Win+Shift+S)" Padding="12,6"/>
            <Button x:Name="PasteBtn" Click="PasteBtn_Click" Content="Paste From Clipboard" Padding="12,6" Margin="8,0,0,0"/>
            <Button x:Name="CopyBtn" Click="CopyBtn_Click" Content="Copy Image" Padding="12,6" Margin="8,0,0,0"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="1" Margin="0,0,0,8" />

        <Border Grid.Row="2" BorderBrush="#DDD" BorderThickness="1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image x:Name="Preview" Stretch="None"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>